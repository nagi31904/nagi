<div class="bg80 center">

    <h2>EDIT</h2>

  <div>
    <%= form_for @work_category, :url => work_category_path(@work_category), :method => :patch do |f| %>
        <%= render :partial => 'form', :locals => {:f => f} %>
        <%= f.submit "Update", :class => "btn btn-default" %>
        <%= link_to "back to show", work_categories_path, :class => "btn btn-default" %>
    <% end %>
  </div>
</div>